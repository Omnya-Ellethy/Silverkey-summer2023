module default {
    type Contact{
        required firstname: str;
        required lastname: str;
        required email: str;
        required title: str;
        required date_of_birth: str;
        required marriagestatus: bool;
        required description: str;
    }

}
